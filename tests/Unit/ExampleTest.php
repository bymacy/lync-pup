<?php

namespace Tests\Unit;

use PHPUnit\Framework\TestCase;

/**
 * Laravel's default scaffold test (asserts true === true) — tests nothing
 * about this app. Left in place with no test methods rather than deleted
 * outright, since this sandbox can't remove files; safe to delete this file
 * entirely.
 */
class ExampleTest extends TestCase
{
    //
}
